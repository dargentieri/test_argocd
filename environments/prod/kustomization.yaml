bases:
    - ../../base
patches:
    - replicacount.yaml
configMapGenerator:
- name: example-config
  namespace: myapp
  #behavior: merge
  files:
  - configs/config.json
patchesStrategicMerge:
    - env.yaml
images:
- name: nginx
  newTag: stable